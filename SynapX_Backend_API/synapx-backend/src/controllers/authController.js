// DEPRECATED — the live auth logic is implemented inline in src/routes/auth.js.
// This controller was an unused second implementation (Redis-backed refresh
// revocation, registerAdmin, changePassword) that diverged from what actually
// runs. Kept as an empty module so nothing breaks if it is still required.
module.exports = {}
