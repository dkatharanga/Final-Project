// DEPRECATED — the live dashboard logic is implemented inline in src/routes/dashboard.js.
// This controller was an unused second implementation (MongoDB-based, cached)
// that diverged from what actually runs. Kept as an empty module so nothing
// breaks if it is still required.
module.exports = {}
