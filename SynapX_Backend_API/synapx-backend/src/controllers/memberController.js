// DEPRECATED — the live member logic is implemented inline in src/routes/members.js.
// This controller was an unused second implementation that diverged from what
// actually runs. Kept as an empty module so nothing breaks if it is still required.
module.exports = {}
